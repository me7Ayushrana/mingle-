export { colors } from './colors';
export { spacing, borderRadius } from './spacing';
export { typography, fontSize, fontWeight } from './typography';
export { moodGradients, defaultGradient, glassOverlay } from './gradients';

export const theme = {
  dark: true,
} as const;
